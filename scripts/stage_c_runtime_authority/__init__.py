"""Stage C21 activation-capable runtime authority core.

This package is deliberately host-agnostic. It contains typed contracts,
checksummed approval records and pure decision logic. It does not invoke
systemd, ALSA, CamillaDSP or production filesystem paths.
"""

SCHEMA_VERSION = 1
